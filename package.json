{
  "name": "omni-executor",
  "private": true,
  "version": "1.0.37",
  "description": "Omni Executor — the Tauri shell. The React app lives in frontend/, the Python backend at the repo root.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
